import { AlphabetsOnlyValidatorDirective } from './alphabets-only-validator.directive';

describe('AlphabetsOnlyValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AlphabetsOnlyValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
